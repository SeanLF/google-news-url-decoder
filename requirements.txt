# Development and test dependencies. NOT the install dependencies.
#
# What a user needs is declared in setup.py, which is the single source of truth:
#
#     pip install googlenewsdecoder            urllib3 + selectolax
#     pip install googlenewsdecoder[async]     adds httpx
#     pip install googlenewsdecoder[socks]     adds PySocks
#
# This file used to pin transitive dependencies (certifi, charset-normalizer, idna, urllib3)
# alongside httpx and PySocks as though all six were required. Nothing reads it -- not setup.py,
# not CI -- so it drifted into stating the opposite of what the package actually installs. Kept,
# but only for the job it can do honestly.

-e .[all]
pytest>=8.0
ruff>=0.16
import-linter>=2.13   # enforces the module layering; contracts live in .importlinter
